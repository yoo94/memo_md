# 개발자 작업 순서: "기능 to another user" 기능 (프론트 관점)

이 파일은 기획/디자인에서 요구한 기능을 개발할 때 개발자가 따라야 할 표준 절차입니다.
간단히 말하면: 마일스톤 → 에픽(큰이슈) → 작은 이슈 → 브랜치 → PR(Closes)→ 코드리뷰 → 머지

---

## 1. 킥오프 (기획/디자인)
- 산출물: 기능 범위(포함/제외), Acceptance Criteria(DoD), 화면 설계(와이어/프로토타입)건

## 2. 마일스톤 생성
- 예: `기능-feature-v1`
- 마감일, 목표(DoD)를 마일스톤 설명에 명확히 기재

## 3. 에픽(issue) 생성 (상위 이슈)
- 제목 예: `EPIC: 기능 to another user`
- 에픽 본문에 범위, DoD, 하위 작업 목록(링크) 명시
- 에픽에 마일스톤 연결

## 4. 작은 이슈들(작업 단위) 생성 및 연결
- 원칙: 하나의 이슈 = 하나 작업 (작게 쪼개기)
- 예시 이슈:
  - UI: 기능 컴포넌트 구현
  - API: 서버(또는 stub) 연동 확인
  - Test: 테스트케이스 ,E2E 시나리오 작성 (성공/실패/네트워크 오류)
  - Docs: ENV/샌드박스 키 설정 문서화
- 각 이슈에 포함할 메타:
  - Acceptance Criteria (완료 조건)
  - 담당자(Assignee)
  - 마일스톤 및 EPIC 링크

## 5. 브랜치 만들기 (이슈별 브랜치)
- 규칙: 브랜치 하나 = 이슈 하나
- 네이밍 예: `feat/기능/<short>-#<issue-number>`
  - 예: `feat/기능/ui-form-#123`

## 6. 개발 & 커밋
- 커밋 메시지는 목적이 명확하게: `feat(기능): implement 기능 form (#123)`
- 민감 정보(키/토큰 등)는 절대 커밋에 포함 금지

## 7. PR 작성
- PR 본문에 반드시 관련 이슈 참조
  - 자동 닫힘을 원하면: `Closes #123` 또는 `Fixes #123` (PR이 Merge 될 때 해당 이슈가 자동으로 닫힙니다)
  - 여러 이슈 닫기: 각 이슈별로 `Closes #123` 줄 추가
  - PR이 이슈의 일부만 해결하면 `Refs #123` 또는 `Partial fix for #123` 사용(이슈 닫지 않음)
- PR 템플릿(권장) 체크리스트 예:
  - [ ] Acceptance Criteria(이슈) 모두 충족
  - [ ] 샌드박스/스테이징에서 기능 검증 완료
  - [ ] 중복 클릭 방지 등 안전장치 구현
  - [ ] 민감 정보 로그 없음 확인
  - [ ] E2E 또는 수동 QA 시나리오 통과
  - [ ] i18n / a11y 점검 완료

## 8. 코드리뷰 → 머지
- 리뷰에서 DoD와 AC가 만족되는지 확인
- PR Merge 시 `Closes`가 포함되어 있으면 관련 이슈 자동 CLOSED

## 9. 스테이징 QA
- 머지 후 스테이징 환경에서 QA 수행
- 필요하면 핫픽스(별도 브랜치/PR) 처리

## 10. 마일스톤 완료 처리
- 마일스톤 페이지에서 필수(p0) 이슈가 모두 닫혔는지 확인
- 완료 시 마일스톤 Close 또는 Release Tag 생성
- 마일스톤 완료 후 회고 이슈 생성(문제/개선점 기록)

## 11. 배포 · 모니터링 · 회고
- 배포 계획(Feature flag 여부, 점진 롤아웃)
- 모니터링: 오류/SLA/사용자 이탈 등 지표 확인
- 배포 후 회고 및 문서 업데이트

---

## 추가 실무 팁
- 여러 PR로 작업을 나눌 때는 중간 PR이 이슈를 닫지 않도록 `Closes`는 최종 PR에만 둡니다.
- 다른 레포에서 이슈를 닫으려면 `owner/repo#NN` 형식 사용.
- 프로젝트 보드 자동화: PR 열리면 "In review"로, Merge되면 "Done"으로 이동 설정 권장.

---
